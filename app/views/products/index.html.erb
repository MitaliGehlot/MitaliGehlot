<%= link_to "New Product", new_category_product_path(@category) %>
All Products:
<ul>
  <% @products.each do |product| %>
    <li>
      <a href="/products/<%= product.id %>">
          <%= product.name %>
      </a>

     <ul><li><%= link_to "Edit", edit_category_path(@product) %>
          <%= link_to "Destroy", category_product_path(@product) %>
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
                </li>
    </ul>
    </li>
  <% end %>
</ul>


