
<ul>
<li>
<%= image_tag @product.file.url , :style => "width: 300px" %>


                                                              
   <p><%= @product.name %>


     Rs. <%= @product.price %>
      <%= link_to "order",   new_product_order_path(@product) %>
      <%= link_to " Edit ",  edit_product_order_path(@product) %>
      <%= link_to "Cancel", product_order_path(@product), method: :delete , data: { confirm: "Are you sure?" } %>
                </li>
  </p>                
  </li>
</ul>

